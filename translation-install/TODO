Things I currently can think of (but probably will not do unless
someone asks for it):
	
 - support command line choosing of language domains

 - add a fallback which simply defaults to the current value of LANG

 - find a better solution to the permission problem than sudo?
 
